# Optional: put overrides here if you want to use docker compose --env-file .env
# This repo works without a .env file, but many teams prefer using one.
#
# Example:
# KONG_TAG=3.13
# POSTGRES_TAG=16
